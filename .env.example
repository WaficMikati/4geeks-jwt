# This file includes global variables that will be available inside your project
# 1. In the front end code you can access this variables like this: console.log(import.meta.env.VARIABLE_NAME)
# 1. In the back end code you access the variable by importing os and then typing print(os.getenv('VARIABLE_NAME'))

# Back-End Variables
# PostgreSQL database connection string
# Format: postgresql://username@host:port/database_name
# For local development with PostgreSQL installed, use:
DATABASE_URL=postgresql://postgres@localhost:5432/example

# Flask secret key for session management (change this to a random string)
FLASK_APP_KEY="change-this-to-a-random-secret-key"

# Flask app entry point
FLASK_APP=src/app.py

# Enable Flask debug mode (set to 0 in production)
FLASK_DEBUG=1
DEBUG=TRUE

# JWT secret key for token generation (change this to a random string)
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=change-this-to-a-random-jwt-secret

# Front-End Variables
# Base path for the application (usually / for root)
VITE_BASENAME=/

# Backend API URL (where the frontend will make requests)
# For local development:
VITE_BACKEND_URL=http://127.0.0.1:3001
